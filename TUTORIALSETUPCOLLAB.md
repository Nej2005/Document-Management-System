clone the repository first either by going vscode > source control > clone repository > clone from GitHub > and find the DMS > folder location should be at ur xampp htdocs.
> Open the cloned DMS folder in VS Code > Open terminal(View > Terminal) and type these in terminal:
pip install -r requirements.txt

For success login, type this to your terminal:
python app.py
